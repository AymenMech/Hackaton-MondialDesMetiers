{% extends ":Back:layout.html.twig" %}

{% block main %}

        <h1>20 dom + Homme</h1>
    {% for domGenderHs in domGenderH %}
        <p>{{ domGenderHs.domain }} : {{ domGenderHs.total }}</p>
    {% endfor %}
        <h1>20 dom + femme</h1>
    {% for domGenderFs in domGenderF %}
        <p>{{ domGenderFs.domain }} : {{ domGenderFs.total }}</p>
    {% endfor %}
    <h1>20 job les + tagés homme</h1>
    {% for jobGenderHs in jobGenderH %}
        <p>{{ jobGenderHs.name }} : {{ jobGenderHs.total }}</p>
    {% endfor %}
    <h1>20 job les + tagés femme</h1>
    {% for jobGenderFs in jobGenderF %}
        <p>{{ jobGenderFs.name }} : {{ jobGenderFs.total }}</p>
    {% endfor %}

    <section class="container-fluid">
        <div class="container">
            <h1 class="back-title">Statistiques d'utilisation de l'application par sexe.</h1>
            <hr class="back-hr">
            <h2>Classement des 20 métiers les plus répondus par homme</h2>
        </div>
        <div class="col-xs-6">
            <table class="table table-striped">
                <thead>
                <tr>
                    <th>Métier</th>
                    <th>Nombre de réponses</th>
                </tr>
                </thead>
                {% for domGenderHs in domGenderH %}
                    <tr>
                        <td>{{ domGenderHs.domain }}</td>
                        <td>{{ domGenderHs.total }}</td>
                    </tr>
                {% endfor %}
            </table>
        </div>
        <div class="col-xs-6 job-canvas"><canvas id="myChart"></canvas></div>
    </section>


    <section class="container-fluid">
        <div class="container">
            <h2>Classement des 20 métiers les plus répondus par Femme</h2>
        </div>
        <div class="col-xs-6">
            <table class="table table-striped">
                <thead>
                <tr>
                    <th>Métier</th>
                    <th>Nombre de réponses</th>
                </tr>
                </thead>
                {% for domGenderFs in domGenderF %}
                    <tr>
                        <td>{{ domGenderFs.domain }}</td>
                        <td>{{ domGenderFs.total }}</td>
                    </tr>
                {% endfor %}
            </table>
        </div>
    </section>

    <script>
        var ctx = document.getElementById('myChart').getContext('2d');
        var myChart = new Chart(ctx, {
            type: 'doughnut',
            data: {
                labels: [
                    "Nombre d'utilisateurs enregistrés",
                    "Nombre d' utilisateurs actifs",
                    "Nombre d'utilisateurs pouvant participer au tirage au sort",
                    "Nombre de métiers ayant reçus des réponses",
                    "Nombre de domaines ayant reçus des réponses"
                ],
                datasets: [{
                    backgroundColor: [
                        "#2ecc71",
                        "#3498db",
                        "#95a5a6",
                        "#9b59b6",
                        "#f1c40f"
                    ],
                    {% for domGenderHs in domGenderH %}
                    data: [{{ domGenderHs.total }}]
                }]{% endfor %}
            }
        });
    </script>
    <script src="{{ asset('js/Chart.js') }}"></script>
{% endblock %}